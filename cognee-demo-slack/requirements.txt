fastapi
uvicorn[standard]
cognee[fastembed] @ git+https://github.com/topoteretes/cognee.git@dev
anthropic
certifi
python-dotenv
python-multipart

# cognee-community-vector-adapter-qdrant is installed separately (see README):
# its pyproject.toml pins an exact `cognee==` version, which fights the dev-branch
# pin above and downgrades pip to a stale adapter build when installed together.
